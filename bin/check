# This file is placed in the Public Domain.

grep -R __import__ bot
grep -R __main__ bot
grep -R __version__ bot
grep -R "## " bot
grep -R popen bot
grep -R pkgutil bot
grep -R importlib bot
grep -R print bot
grep -R "import sys" bot
grep -R "import inspect" bot


grep -R __import__ op
grep -R __main__ op
grep -R __version__ op
grep -R "## " op
grep -R popen op
grep -R pkgutil op
grep -R importlib op
grep -R print op
grep -R "import sys" op
grep -R "import inspect" op
