#!/usr/bin/env python3
# This file is placed in the Public Domain.

import os, sys ; sys.path.insert(0, os.getcwd())

import inspect
import pkgutil

from bot.obj import getcls, items
from bot.tbl import Table

import bot.all

tbl = Table()
tbl.scan("bot")


print("# This file is placed in the Public Domain.")
print("")
print("from bot.tbl import Table")
print("")
print("tbl = Table()")
print("")
print("tbl.classes.update(%s)" % tbl.classes)
print("for k, cn in items(tbl.classes):")
print("print(k, cn)")
print("print(getcls(cn))")
print("tbl.modnames.update(%s)" % tbl.modnames)
print("tbl.names.update(%s)" % tbl.names)